{
  "name": "ai-document-qa-bot",
  "private": true,
 "scripts": {
    "dev": "turbo run dev",
    "dev:api": "cd apps/api && uvicorn app.main:app --reload",
    "dev:web": "cd apps/web && pnpm dev",
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down"
  },
  "devDependencies": {
    "prettier": "^3.7.4",
    "turbo": "^2.8.2",
    "typescript": "5.9.2"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=18"
  }
}
